namespace UmaFestHub.Domain.Entities;
//DurationUnit enum to specify the unit of time 
//for a Duration value (e.g., minutes, hours, days).

public enum DurationUnit
{
	Minutes = 0,
	Hours = 1,
	Days = 2
}
